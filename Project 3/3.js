let isSunny = true;
let isWarm = true;

if (isSunny || isWarm) 
    {
    console.log("It's either sunny or warm (or both).")
    } 
    else
    {
    console.log("It's neither sunny nor warm.")
    }
